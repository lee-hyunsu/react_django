import React, { Component } from 'react';

export default class RoomJonPage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>This is the chatbot room join page</p>
    }
}